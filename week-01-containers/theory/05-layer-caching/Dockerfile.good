FROM node:20-alpine
WORKDIR /app

# GOOD: Copy dependency manifest first — npm ci is cached until package.json changes
COPY package.json package-lock.json* ./
RUN npm ci

# Source code goes last — only this and build re-run on code changes
COPY . .
RUN npm run build

EXPOSE 3000
CMD ["npm", "start"]
