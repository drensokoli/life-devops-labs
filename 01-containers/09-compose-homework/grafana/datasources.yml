apiVersion: 1
datasources:
  - name: Loki
    type: loki
    access: proxy
    # Use the compose service name "loki", not localhost — Grafana reaches
    # Loki across the shared docker network on its container port 3100.
    url: http://loki:3100
    isDefault: true
