apiVersion: 1
datasources:
  - name: Loki
    type: loki
    access: proxy
    # Use the compose service name — Grafana reaches Loki over the
    # internal network, not localhost.
    url: http://loki:3100
    isDefault: true
