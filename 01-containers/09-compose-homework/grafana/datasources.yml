apiVersion: 1
datasources:
  - name: Loki
    type: loki
    access: proxy
    # Use the compose service name (NOT localhost) so Grafana reaches Loki
    # over the compose network.
    url: http://loki:3100
    isDefault: true
